/**
 * ==========================================
 * JavaScript 조건문 완벽 정리 (if / switch)
 * ==========================================
 * - if / else if / else
 * - 조건 평가 순서
 * - switch 문 동작 원리
 * - 언제 if, 언제 switch를 쓰는지
 */

// ==========================================
// 1. if 문 기본 구조
// ==========================================
console.log('--- if 기본 ---');

/**
 * if 문은 조건이 true인지 판단
 * 조건식은 반드시 boolean으로 평가됨
 */

let number = 5;

if (number % 2 === 0) {
   console.log('number 변수는 짝수입니다');
} else {
   console.log('number 변수는 홀수입니다');
}

/**
 * 실행 흐름
 * number % 2 === 0 → false
 * → else 블록 실행
 */

console.log('='.repeat(40));

// ==========================================
// 2. if / else if / else 체인
// ==========================================
console.log('--- if / else if ---');

/**
 * 조건은 위에서부터 순서대로 검사
 * 처음 true가 되는 블록만 실행
 * 이후 조건은 검사하지 않음
 */

if (number % 2 === 0) {
   console.log('2의 배수입니다');
} else if (number % 3 === 0) {
   console.log('3의 배수입니다');
} else if (number % 4 === 0) {
   console.log('4의 배수입니다');
} else if (number % 5 === 0) {
   console.log('5의 배수입니다');
} else {
   console.log('모두 아닙니다');
}

/**
 * 현재 number = 5
 *
 * 검사 순서
 * 1️⃣ 2의 배수 ❌
 * 2️⃣ 3의 배수 ❌
 * 3️⃣ 4의 배수 ❌
 * 4️⃣ 5의 배수 ✅
 *
 * 👉 여기서 종료
 */

console.log('='.repeat(40));

// ==========================================
// 3. if 문 사용 시 주의점
// ==========================================
console.log('--- if 주의점 ---');

/**
 * 조건 순서가 매우 중요
 */

let score = 85;

if (score >= 60) {
   console.log('합격');
} else if (score >= 90) {
   console.log('우수');
}

/**
 * 위 코드는 논리 오류 ❌
 * 90점도 60점 이상이므로 첫 if에서 끝남
 *
 * 올바른 순서:
 * if (score >= 90) → else if (score >= 60)
 */

console.log('='.repeat(40));

// ==========================================
// 4. switch 문 기본 구조
// ==========================================
console.log('--- switch 기본 ---');

/**
 * switch는 "값 비교"에 특화
 * 내부적으로 === (엄격 비교) 사용
 */

const englishDay = 'monday';
let koreanDay;

switch (englishDay) {
   case 'monday':
      koreanDay = '월요일';
      break;

   case 'tuesday':
      koreanDay = '화요일';
      break;

   default:
      koreanDay = '주말';
      break;
}

console.log(koreanDay);

/**
 * 동작 방식
 * switch (englishDay)
 * → case 값들과 === 비교
 * → 일치하는 case부터 실행
 *
 * break 없으면 아래로 계속 실행됨 ❗
 */

console.log('='.repeat(40));

// ==========================================
// 5. break가 없는 switch (주의)
// ==========================================
console.log('--- break 없음 ---');

let grade = 'B';
let message = '';

switch (grade) {
   case 'A':
      message += '우수 ';
   case 'B':
      message += '양호 ';
   case 'C':
      message += '보통 ';
      break;
   default:
      message += '재수강';
}

console.log(message);

/**
 * grade = 'B'
 *
 * 실행 흐름
 * case 'B' → 실행
 * break 없음 → case 'C' 실행
 *
 * 👉 의도적인 fall-through 아니면 break 필수
 */

console.log('='.repeat(40));

// ==========================================
// 6. if vs switch 언제 쓰나
// ==========================================
console.log('--- if vs switch ---');

/**
 * if 사용이 좋은 경우
 * - 범위 비교 (>, <, >=, <=)
 * - 복합 조건 (&&, ||)
 * - 조건식이 복잡할 때
 *
 * switch 사용이 좋은 경우
 * - 특정 값 비교
 * - 경우의 수가 명확
 * - 문자열 / 상수 분기
 */

console.log('조건문 정리 완료!');

console.log('='.repeat(40));

// ==========================================
// 7. 핵심 요약
// ==========================================
/**
 * ✅ if
 * - 조건식 기반
 * - 위에서부터 순차 평가
 * - 가장 유연함
 *
 * ✅ switch
 * - 값 비교 특화
 * - === 비교
 * - break 필수
 *
 * ⚠️ 실무 팁
 * - 조건 많아지면 가독성 먼저 고려
 * - switch 남용 ❌
 * - 로직 복잡하면 if가 낫다
 */

// ==========================================
// 8. 2026년 실무 조건문 패턴
// ==========================================
console.log('--- 2026 실무 조건문 패턴 ---');

/**
 * 📌 2026 기준 핵심 흐름
 * - 조건문 문법은 그대로
 * - "중첩 제거" + "의도 드러내기"가 핵심
 * - if / switch를 덜 쓰는 방향으로 진화
 */

console.log('='.repeat(40));

// ==========================================
// 8-1. Early Return 패턴 (실무 최중요)
// ==========================================
console.log('--- Early Return ---');

/**
 * ❌ 중첩 if (가독성 최악)
 */
function validateUser_bad(user) {
   if (user) {
      if (user.isActive) {
         if (user.age >= 18) {
            return true;
         }
      }
   }
   return false;
}

/**
 * ✅ 실무 표준 패턴 (Early Return)
 */
function validateUser(user) {
   if (!user) return false;
   if (!user.isActive) return false;
   if (user.age < 18) return false;

   return true;
}

console.log(validateUser({ isActive: true, age: 20 }));

/**
 * 장점
 * - 중첩 제거
 * - 읽는 순서 = 검증 순서
 * - 유지보수 쉬움
 */

console.log('='.repeat(40));

// ==========================================
// 8-2. 조건문 + 논리 연산자 패턴
// ==========================================
console.log('--- 논리 연산자 조건 처리 ---');

/**
 * 단순 조건 실행은 if 없이 처리
 */

const isLoggedIn = true;
const hasToken = true;

if (isLoggedIn && hasToken) {
   console.log('접근 허용');
}

// 실무에서는 아래 패턴도 매우 흔함
isLoggedIn && hasToken && console.log('접근 허용');

/**
 * ⚠️ 주의
 * - 조건이 2개 초과되면 다시 if 사용
 */

console.log('='.repeat(40));

// ==========================================
// 8-3. switch 대체 패턴 (Object Map)
// ==========================================
console.log('--- switch 대체 패턴 ---');

/**
 * switch → 객체 매핑으로 대체
 */

const dayMap = {
   monday: '월요일',
   tuesday: '화요일',
   wednesday: '수요일',
};

const today = 'monday';
const dayResult = dayMap[today] || '주말';

console.log(dayResult);

/**
 * 장점
 * - switch 제거
 * - 추가/수정 쉬움
 * - React에서 특히 많이 사용
 */

console.log('='.repeat(40));

// ==========================================
// 8-4. 조건 판단과 실행 분리
// ==========================================
console.log('--- 조건과 행동 분리 ---');

/**
 * 조건 판단 → boolean 반환
 * 실행 로직 → 별도 함수
 */

function isAdult(user) {
   return user.age >= 18;
}

function allowAccess() {
   console.log('접근 허용');
}

const user = { age: 22 };

if (isAdult(user)) {
   allowAccess();
}

/**
 * 장점
 * - 테스트 쉬움
 * - 의미가 코드에 드러남
 */

console.log('='.repeat(40));

// ==========================================
// 8-5. 프론트엔드 UI 조건 렌더링 패턴
// ==========================================
console.log('--- UI 조건 처리 사고 ---');

/**
 * UI는 우선순위가 핵심
 * → Early Return 기반 처리
 */

function renderUI({ isLoading, error, data }) {
   if (isLoading) return '로딩중';
   if (error) return '에러 발생';
   if (!data) return '데이터 없음';

   return '정상 화면';
}

console.log(renderUI({ isLoading: false, error: null, data: {} }));

console.log('='.repeat(40));

// ==========================================
// 9. 실무 기준 최종 요약 (2026)
// ==========================================
/**
 * ✅ 2026 실무 조건문 원칙
 *
 * 1️⃣ 중첩 if는 실패 신호
 *    → Early Return으로 평탄화
 *
 * 2️⃣ switch는 줄일 수 있다
 *    → Object Map 고려
 *
 * 3️⃣ 조건 판단과 실행은 분리
 *
 * 4️⃣ 읽히는 순서 = 실행 순서
 *
 * 5️⃣ 조건문은 제어문이 아니라
 *    "의사결정 문서"
 */

console.log('2026 조건문 실무 패턴까지 완료!');
console.log('='.repeat(40));
