/**
 * =====================================================================
 * JavaScript Basic: 원시 자료형 (Primitive Types) - Executable Note
 * =====================================================================
 * ✅ 핵심 요약
 * 1. Number: 정수, 실수 구분 없이 모두 number. (Infinity, NaN 주의)
 * 2. String: 문자열. 실무에선 백틱(``)을 쓴 템플릿 리터럴이 표준. ⭐
 * 3. Boolean: true / false. (조건문, 리액트 조건부 렌더링의 핵심)
 * 4. Null vs Undefined:
 *    - undefined: "아직 값이 할당되지 않음" (자바스크립트 엔진이 설정)
 *    - null: "값이 없음을 명시적으로 표현" (개발자가 설정) 🛡️
 */

console.clear?.();
const line = () => console.log('-------------------------------------');

// =====================================================================
// 1) Number Type (숫자)
// =====================================================================
{
   console.log('1. Number Type');

   let num1 = 28;
   let num2 = 1.5; // 실수도 그냥 number
   let num3 = -20;

   // 사칙연산
   console.log(`더하기: ${num1 + num2}`);
   console.log(`빼기: ${num1 - num2}`);
   console.log(`곱하기: ${num1 * num2}`);
   console.log(`나누기: ${num1 / num2}`);
   console.log(`나머지: ${num1 % num2}`); // 홀/짝수 판별할 때 자주 씀

   // 🔥 특수 숫자값 (Error Handling 시 중요)
   let inf = Infinity; // 양의 무한대
   let mInf = -Infinity; // 음의 무한대
   let nan = NaN; // Not a Number (수학적으로 계산 불가능)

   console.log(1 * 'hello'); // 결과: NaN (숫자랑 문자를 곱하면 망함)

   // 🛡️ 실무 팁: NaN인지 확인할 때는 isNaN() 함수를 씁니다.
}

line();

// =====================================================================
// 2) String Type (문자열)
// =====================================================================
{
   console.log('2. String Type');

   let myName = '신재준';
   let myLocation = '일산';

   // [Old] 문자열 연결 (+ 연산자)
   let introduce = myName + myLocation;
   console.log('Old Way:', introduce);

   // [Modern] 템플릿 리터럴 (Template Literal) ⭐⭐⭐
   // 백틱(`)을 사용하고 ${} 안에 변수를 넣음.
   // 리액트에서 className 동적 할당할 때 필수 스킬!
   let introduceText = `${myName}은 ${myLocation}에 살고 있습니다.`;
   console.log('Modern Way:', introduceText);
}

line();

// =====================================================================
// 3) Boolean Type (논리형)
// =====================================================================
{
   console.log('3. Boolean Type');

   let isSwitchOn = true;
   let isEmpty = false;

   console.log('스위치 켜짐?', isSwitchOn);
   console.log('비었음?', isEmpty);

   /**
    * 💡 리액트 활용:
    * {isSwitchOn && <Component />}
    * -> true면 보이고, false면 안 보이는 패턴에 쓰임.
    */
}

line();

// =====================================================================
// 4) Null vs Undefined (면접 빈출 🔥)
// =====================================================================
{
   console.log('4. Null vs Undefined');

   // 1. undefined: 변수를 선언만 하고 값을 안 넣었을 때
   let none;
   console.log('값 할당 안 함:', none); // undefined

   // 2. null: 개발자가 "여긴 비어있다"고 명확히 표시할 때
   let empty = null;
   console.log('의도적으로 비움:', empty); // null

   // ⚠️ 주의: typeof null은 'object'로 나옵니다 (JS의 유명한 버그)
   console.log('null의 타입:', typeof null);
}
